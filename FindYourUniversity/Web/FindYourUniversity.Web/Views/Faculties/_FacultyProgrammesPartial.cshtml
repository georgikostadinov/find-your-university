@model IEnumerable<FindYourUniversity.Web.ViewModels.ProgrammeLinkViewModel>

<ul class="nav">
    @foreach (var item in @Model)
    {
        <li>@Html.ActionLink(item.Name, "Details", "Programmes", new { id = item.Id }, new { @class="btn btn-default" })</li>
    }
</ul>